nav.navbar.navbar-expand-lg.navbar-light.bg-light
  a.navbar-brand(href="/usuario/home") LIS
  ul.navbar-nav.ml-auto
    li.nav-item
      a.nav-link(href="/orden/all") Órdenes
    li.nav-item
      a.nav-link(href="/paciente/busqueda") Crear orden
    li.nav-item
      a.nav-link(href="/orden/pendientes/muestras") Registrar muestra

    // Mostrar "Registrar resultados" solo si rol es administrativo o técnico
    if role === 'administrativo' || role === 'tecnico'
      li.nav-item
        a.nav-link(href="/orden/preinforme") Registrar resultados

    // Mostrar "Validar resultados" solo si rol es administrativo o bioquimico
    if role === 'administrativo' || role === 'bioquimico'
      li.nav-item
        a.nav-link(href="/orden/validacion/lista") Validar resultados

    // Opciones de Administrador solo si rol es administrativo
    if role === 'administrativo'
      li.nav-item.dropdown
        a.nav-link.dropdown-toggle(href="#", data-toggle="dropdown") Administrador
        .dropdown-menu
          a.dropdown-item(href="/auditoria") Auditoria
          a.dropdown-item(href="/orden/historial") Historial de órdenes
          a.dropdown-item(href="/cargar_datos/") Cargar exámenes nuevos
          a.dropdown-item(href="/usuario/all") Administrar Usuarios

    li.nav-item
      a.nav-link(href="/usuario/perfil") Perfil
    li.nav-item
      a.nav-link(href="/auth/logout") Cerrar Sesión
